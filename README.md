# Umbra

Umbra is a plugin for the Entire CLI that finds the code an AI agent's change affects but the agent never looked at, and renders it as an eclipse map with an attention replay. See [umbra/](umbra/).

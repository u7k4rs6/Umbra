You are starting the build of Umbra, a plugin for the Entire CLI that finds the code an AI agent's change affects but the agent never looked at, and renders it as an eclipse map with an attention replay. The design is complete. Your job is to execute it, not to redesign it.

Setup you can assume: you are inside a fresh fork of entireio/entire-graph with Entire enabled (`entire status` works), the entire-graph plugin installed (`entire graph capabilities --json` works), and the directory `umbra/` containing HANDOFF.md, KICKOFF_PROMPT.md and docs/. If any of that is missing, say so and stop.

Read in this order before doing anything else: umbra/HANDOFF.md, umbra/docs/PRD.md, umbra/docs/ARCHITECTURE.md, umbra/docs/SECURITY_AND_ACCESS.md, umbra/docs/FRONTEND_SPEC.md. Then run `entire graph agent-guide` and follow its search-first workflow for every investigation in this repository.

Rules for the whole build:
- Do not reopen the settled decisions in HANDOFF.md. Go, single binary `entire-umbra`, own module under umbra/, inputs only through documented Entire and Graph output, four boundaries behind interfaces, one Runner for all external processes, no model anywhere.
- Four states (lit, penumbra, umbra, unknown) exactly as defined, with the five penumbra tiers (glance, glimpse, quoted, afterimage, echo); missing data is the unknown state, never an error. Six ranking factors, the beacon override, the opt-in scar factor, and the default full sweep with leak reasons are all specified; implement them as written. Umbra never executes commands from a transcript; test IDs are validated and passed as argv.
- Layout is computed in Go and stored in the JSON; the JS draws from those positions and never computes layout.
- Frontend: vanilla JS and CSS only, no libraries, no fonts, no requests, no motion on load. Follow FRONTEND_SPEC.md exactly where it is exact, including the Vocabulary section: every coined word on the page sits next to its plain meaning.
- No em dashes in any file you write, code comments included. Plain language.
- Tests replay recorded fixtures; no test may need a live agent or network. `go test ./...` must be green at every commit.
- Commit after every phase. Message format: `umbra: phase N <short summary> (tests: <count>)`. Do not squash. Touch nothing outside umbra/ except a one-line pointer in the repository README.
- Before changing a file you did not create, run `entire graph impact` on the symbols you are about to touch and note callers in your working notes.
- Keep the machine light: one agent session, no parallel builds, no background processes left running.

Do this now, in order:

1. Step 0, the probe (ARCHITECTURE.md, "Step 0"). Create umbra/fixtures/app as described (Python service with app/service.py, app/api.py, app/refunds.py, app/models.py, four pytest files, a venv-based setup script, and a `compute_total` function called from api.py, refunds.py and tests/test_service.py). Run one short session on it that reads app/service.py fully, reads app/api.py with an offset and limit that miss its caller, greps for compute_total, mentions apply_refund by name in its reply without opening app/refunds.py, changes the signature of compute_total, runs `pytest tests/test_api.py`, and commits with a message that says the callers were checked. Dump the checkpoint (`entire checkpoint list --json`, `entire checkpoint explain <id> --raw-transcript`, `--full`), inspect `entire checkpoint explain <id> --short`, `entire graph capabilities --json`, `entire graph snapshot --repo . --format ndjson | head -50`, `entire graph checkpoint <id>`, `entire graph commit HEAD --repo .`, `entire graph impact --symbol compute_total --repo .` (confirm call-site lines), and `entire graph verify --repo . --test "pytest -q tests/test_api.py"`, plus each command's `--help` for JSON flags. Answer the eight open questions at the bottom of ARCHITECTURE.md by editing that section. Apply the degradation table for any "no". Commit as phase 1.

2. Phases 2 through 5 from ARCHITECTURE.md "Build order": skeleton with Runner, resolve-by-trailer and worktrees; the transcript adapter with offset, limit, grep paths, mentions, the session-said line and the timeline; the field (snapshot loader, relations mapping from capabilities, BFS, impact parser with call-site lines, sources from `graph checkpoint` with `commit` fallback); the classifier with all penumbra tiers, the call-site window, the six-factor ranker with the beacon override, and the terminal table. At the end of phase 5, `entire umbra HEAD --run none --no-audit` must print the table on the fixture app with lit, penumbra (at least glance and echo) and umbra rows and the session-said line. Commit each phase.

3. Stop after phase 5 and write umbra/NOTES.md: probe findings, degradations in effect, the table output, test count, anything in the docs that turned out to be wrong. Then continue with phases 6 and 7 (test selection and execution through verify, the default sweep with leak forensics, `--history`, JSON, the packet, recordings and replay tests for all eight scenarios).

4. Before phase 8, re-read FRONTEND_SPEC.md in full. Build the light field first (background, ring guides, radial glow, shadow mask with blurred pools) and screenshot it before drawing nodes, edges or labels. Then nodes, edges, labels, docket, detail panel (phase 8, stable checkpoint), then replay, keyboard, spotlight, day scheme and print (phase 9). Run the QA checklist and record results in NOTES.md. Then phases 10 and 11.

If a constraint change is announced during the build, read the "Constraint-change playbook" in ARCHITECTURE.md first, write a short plan into NOTES.md before editing, implement it inside the boundary named there, and commit it as its own phase.